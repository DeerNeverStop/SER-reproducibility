# 完整中文工作稿的实际排版检查

本目录是明确保留结果占位的**工作稿**，不是最终双语论文。可编辑叙事源为上一层 `full_corresponding_draft_zh.md`，对应英文五页占位稿 `../page_budget/SYNTHETIC_main.tex`。C1–C8方法沿用已审精简中文版；引言、历史研究关系、限制与AI披露另经独立对应审阅。根任务修正了中文中比英文更强的“公认评估要求”表述，并用等义短句消除页末两个字的断行。

`WORKING_rendered_zh.md` 是实际编译输入的原字节副本。它移除编辑注释，把引用键映射到当前英文 bibliography 的12条编号，补入该英文书目的完整记录，并在标题加工作稿标识。该编号按当前英文书目顺序，不构成最终编号锁；整篇英文最终改写后，两版仍须同步引用顺序，更新包含新研究的 artifact 条目。它没有真实结果图，也没有把英文 SYNTHETIC 曲线的假数据带入中文；六项主表的24个结果格和三库绝对表的18个格仍为“待填”。

根任务实际用中文构建器生成8页PDF，共198,222字节，SHA256为 `1d063d5e20ca36e7dc8f0df53f450fe49e9b2b2f8ffd8d17d8e47347e34c558c`。本机产物：`D:/SER-final-program-20260907/paper_layout_checks/WORKING_full_chinese_v2/working_full_zh.pdf`。初版v1也保留。最新正文第1–6页、参考文献第7–8页；中文没有固定页数要求。

同时修复中文生成器的旧硬编码PDF标题：现在由源文第一条一级标题获得实际可见文本，写入PDF元数据和构建收据，作者仍为Tian Xie。实际构建器SHA256为 `d6f2569309c354aa4a5b7220871b31457a197babd29f9ee0b0ee2deeb119b7c3`。该文件在进行中正式训练与后处理合同的66项pins之外，没有改变任何实验来源文件。既有图形嵌入QA仍是对应旧字节的历史记录；本次未插入图，不能用本次通过替代最终真实图嵌入验收。

机器检查重新针对v2 PDF执行：全部字体嵌入、无Type3字体和不可见文字模式、无加密、页内字形边界、全部页眉页脚、正确作者/语言、元数据标题与源文一致，未发现编辑注释或引用键残留。公式δ、集合符号和E均可提取，三库表格及12条引用完整。`STRUCTURAL_QA.json` 保留检查范围和限制，不声称自动视觉通过。

根任务实际查看v1全部8页的120dpi渲染，再查看v2修改后的第5页；其余7页新渲染PNG与已查看版本字节SHA完全相同。未见表格/公式裁切、重叠或缺字，页末孤字已消除。完整记录在 `VISUAL_QA.json`。新科学数据和最终表述进入后，两版仍需重新编译、核数、逐页检查及打包。

复现正文布局可从仓库根目录执行现有构建器，将本目录的 `WORKING_rendered_zh.md` 传给 `--source`，使用一个新的 `--output` PDF路径；运行时需要ReportLab、pypdf及本机SimSun/SimHei字体。PDF时间戳可能变化，不能期待重建PDF字节与本次完全相同。`PREPARATION.json`、`BUILD_RECEIPT.json` 和两份QA记录保留本次真实输入/输出指纹。
