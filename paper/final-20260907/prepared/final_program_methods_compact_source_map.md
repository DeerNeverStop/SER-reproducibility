# 方法精简备选与去向（2026-09-07）

这是四页正文排版用的替代片段，未写入原 `main.tex`，未改已验收方法片段、冻结协议或任何科学输出。英文约 **658 词**，同一粗计词法下原片段约 **815 词**，净减约 **157 词**；表和展示公式另计，行内数学计一个词，去除注释及引用命令。不同 TeX 计词器可有少量差异。中文 C1–C8 与英文逐段对应。

- 英文：`final_program_methods_compact_en.tex`
- 中文：`final_program_methods_compact_zh.md`
- 细化科学依据：原 `final_program_methods_source_map.md`；其冻结代码定位与本备选共用。

首版六页主要存在双栏浮动队列问题；前移表/图后，根代理已确认两表在第三页、曲线在第四页，正文只需约 100–150 词余量。因此本备选保留三库表全部数值、六项检验义务和曲线描述，没有依据未见结果决定删除图表，也不采用此前更激进的 535 词设想。排版是否真正四页，由根代理使用明确 SYNTHETIC 占位再次编译核验。

| 对应段 | 精简方式与保留事实 | 移到既有完整记录的细节及原始依据 |
|---|---|---|
| C1 / M1 | 合并范围句，保留 360 A＋24 B、24 draw×5 fold、原生类数与人数、既往接触/私有冻结。 | 无科学条件删除。见 `SCIENCE_DESIGN.md` 与 `plan.py:19`、`:25`、`:180`。 |
| C2 / M2、表 | 缩短角色分配句，保留 A/B/outer 人物互斥、元数据分层、共同 query 文本与 fit 不交、代表 take、缺格门槛；原表所有数值与外测脚注保留。 | 原表注“不是训练更新次数”并入 distinct recordings 的含义；具体哈希/候选排序见 `plan.py:137`–`:177`。 |
| C3 / M3 | 合并模型与优化句；完整保留 cfg3、top4/native head、AdamW 两学习率/WD、无权 CE、15 完整轮、无早停/搜索、FP16/FP32、3 s/10 s/16 kHz/30 dB。 | 删除正文对 FP16 波形缓存再转 FP32 的展开；完整 `v3/deploy/engines_deploy.py:187` 与 `engine.py:67`–`:85` 仍提供精确处理路径。 |
| C4 / M4 | 保留同轨迹四规则、CE/UAR、最早精确 ties、whole-group macro recall、三角色固定独立 batch16、无长度 mask 的 padding 限制。 | RNG 保存与每轮恢复 train mode 是未变的工程保证，详见 `engine.py:106`–`:128`、`:163`–`:218`；有理数 tie 保证仍留正文，详见 `score.py:51`–`:77`。 |
| C5 / M5 | 三条式合为一般 `δ_q` 加 `J` 两行，符号/方向/pp 完全等价。保留非 loss、非 V−T、非单一机制和四规则只需一条轨迹。 | 无科学估计量删除。见 `score.py:174`–`:203`。 |
| C6 / M6 | 合并统计句；保留五折等权→24 draw、df23、双侧 t、固定六族/Holm、pointwise95%、零方差 NA 占位、δUAR 描述、1 pp 非等效、有限语料/非语言/无额外重复。 | “不做 bootstrap”的显式句留完整分析规范，未换用其他区间或检验；见 `plan.py:36`–`:49`、`score.py:131`–`:160`、`:218`–`:270`。 |
| C7 / M7 | 合并配对随机化名称，保留共享初始权重/随机日程、sex/rank/text/class/take/intensity 槽、24 fold0 B、last15、E 原式及仅描述、整组替换/query 用于 A 选择。 | 初始化/排序/裁剪/训练四流的名字及 SHA 裁剪映射见 `plan.py:70`–`:79`、`:137`–`:177`；不改变匹配合同。 |
| C8 / M8 | 保留四 pilot 排除、384 完整核验门槛、全15轮曲线/选中轮次与一致性/last/middle–late/oracle 的全部预定描述及 oracle 不选模。 | 去重 delta、末轮状态保存、磁盘回载最大 logits 误差 ≤10^-5 移完整报告，详见 `engine.py:225`–`:260`；完整 gate 在 `run.py:421`–`:458`。 |

所有源码相对路径以上述 `v3/final_program_20260907/` 为基准，唯显式给出 `v3/deploy/` 的旧依赖除外。原理表述没有根据正在运行的正式数据做选择；此备选没有实际完成声明。未减少正式拟合、检查点规则、终点、检验族、诊断或报告责任。
