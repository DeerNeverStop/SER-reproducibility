# 2026-09-05：Study II 执行进展

> **公开投影（2026-09-08）**：本文件删除了账户、运维身份或私有审阅入口；科学方法与结果保留。原始文稿存于受控私有归档，原 SHA-256 为 `d3256c5c3615e976ade8185b6a5c50f594f06cdab3bcef17fc600de4010aac8b`。公开版不是原冻结文件的字节副本；原科学代码、结果与计划未在此改写。

## 完整训练、独立核验与停止GPU（11:48，多伦多）

Study II **1,440/1,440** 单元完成：Ridge720、CNN720，全部首次尝试成功。云端CNN最后单元15:31:15Z完成，full.status=0，无runner/锁。最终720单元备份2,168文件全部校验，已在15:43Z停止Pod并独立确认EXITED；保留磁盘，计算按标价时长粗估$2.32，最终账单另核，存储仍可能计费。没有自动重启或追加实例。

新离线merged_core保留两源原账本、环境和工件。全部1440单元完整性闭合后才运行评分器；独立复算通过6,199数值，最大误差2.13×10⁻¹⁴个百分点。16个冻结文件哈希保持不变，没有看部分结果调设计。新的完整执行回执为 core_execution_20260905.json（未随此公开快照分发；原件另行保留）。

最主要的四个陌生人＋新句配置差（S48−S12，UAR百分点，逐项95%条件性区间）：Ridge/B288 +0.73 [0.07,1.43]；Ridge/B576 +0.06 [−0.64,0.76]；CNN/B288 +1.08 [0.50,1.67]；CNN/B576 +2.16 [1.54,2.78]。CNN每个预算的三个draw方向均为正，Ridge两个预算均有draw变号。四个DID区间均跨零，不能断言新句更受益或两条件等效。

研究支持在本语料和训练制度下，人数/每人句子覆盖的取舍具有模型相关的收益；不能把它包装成最优数据集、通用建议器或真实客服收益。所有16绝对量、12对比和draw补表进入稿件与图表，不筛选有利结果。两模型前端与训练方式不同，跨模型现象不能单独归因架构。

实际运行发现离线合并CLI的Windows64位句柄声明问题，已在未冻结工具修复；两项真实Windows子进程测试通过，实际合并随后成功。新图表工具在TEST审阅中补强同一成绩字节先验SHA再解析的门禁，12项TEST通过；不改训练或统计源。既有122测试、合并24测试、备份5测试均为先前批次结果，未声称一次合并跑完全部。

旧N14R只读观察11:48时为1119/1920，三个失败均已有后续成功；未写闭合。按近期完成速度粗估剩21.5–24.9小时，原owner继续管理。新研究不依赖它释放GPU。

已完成图表视觉审查、英文完整结果/讨论、中文解释与20文件便携结果包；全部28量、4个主对比draw表及稿件链接已逐项核对。后续由作者审阅并整合到旧主线及会议篇幅。当前新增实验计算已提前完成，9/16新增研究的算力风险已明显降低；这不等于整篇论文已可投稿。

以下是阶段历史，旧“等待GPU/尚无结果”描述仅对当时有效。

## 云端备份与合并工具（09:53 快照，多伦多）

已完成 143 个 CNN 单元的云端快照打包和下载；外层 SHA 一致，恢复到独立本地快照目录后，437 个文件全部通过 SHA 核验。运行仍在继续，快照保留活动锁，不能用于评分。云端运行回执（未随此公开快照分发；原件另行保留）记录环境、计划及备份身份。

新增不解析预测数组的云备份工具与离线合并工具；后者只接收各 720 单元的完整离线来源，保留原账本与环境，写入全新目录并核验 1,440 单元闭合。新增 24 项合并 TEST 测试和 5 项备份 TEST 测试全部通过；此前 122 项程序测试已通过。尚未运行真实合并或科学评分。16 个冻结源文件/规范保持原哈希。

现有每小时心跳的旧提示发生中文编码损坏，已通过应用结构化接口重写为可读文本，保留目标任务、每小时频率、既有实例授权与停机时限。新提示明确不在本机重复启动 CNN、不使用本地 Ridge-only 目录误报云端进度。正常运行无额外用户操作。

## 云端推进补记（09:35，多伦多）

独立云端 CNN 已启动，连接与 CUDA 技术检查通过；冻结16项源文件/计划/特征核验及云端122项测试通过。新CNN使用独立PyTorch2.8/CUDA12.8环境，源码/科学配置不变；旧N14R无改动。个人授权、充值和实例身份只保留于受控原件。

首批8个正式CNN全部成功，0失败；平均9.1367秒，覆盖B288/576、seen/new，但只代表首个fold/draw/rotation，不能当作全量速度保证。这8个已备份本地，SHA256一致。随后同主机同目录续跑剩余712个，已观察至少2个新增单元成功，PID541。只读取元数据及耗时，未看科学分数。下文“CNN等待本机GPU”是旧状态，现以云端运行记录为准。

详细操作与成本保护见工作区 `runpod_setup/运行说明.md`、`Runpod_5090当前实例.json`。现有StudyII心跳已更新，负责云端巡检、备份、完成/失败后停止GPU，最迟2026-09-06T16:00Z停止计算。不要再在本机重复启动CNN。云端块完成后仍需合并既有Ridge工件、完整闭合和独立核验，不能提前评分。

## 早期 CPU 阶段记录：完成事项与工件

已读取 GitHub Claude PR #7，按用户“新增研究也尽量赶 9/16”调整为固定预算配置核心。完成独立 worktree、真实清单/缓存核验、强度修正、性别分层、精确配额和句子曝光控制。代码、科学规范、压缩完整计划、独立元数据验证和工程先导回执已在正式训练前推送，提交 `6bf62b9`。

数据检查支持全部 1,440 单元；每模型 720 次，全部 91 人和 12 句均参与预定测试轮换。早停实际 279–288 条，缺失按真实清单保留，不补造。代表单元 6,524；唯一因缺 MD/XX 而不能用的是 `1040_ITH_SAD_X.wav` 对应单元。

## 新增运行量和耗时

- 工程先导：9/9 次 CPU，进程内墙钟 2.34 秒，只核验身份和耗时。
- 正式 Ridge：**720/720 次已完成，0 次失败**，逐单元 DONE、回执、路径和预测哈希复核通过。runner start→stop 92.12 秒，不含前置校验；累计单位墙钟 65.51 秒、累计拟合 61.10 秒。
- 采用两 CPU 线程、BelowNormal、CUDA 不可见且未初始化。旧 N14R 继续正常运行，新任务未使用 GPU。
- 冻结前 71 项测试通过，包括预算、泄漏、恢复、尝试上限和统计夹具。独立评分审阅提出的账本核验遗漏已修复并复审关闭；最终 **85 项测试通过**，16 个冻结源码/规范文件保持原哈希。评分补强未改变已锁定训练计划。

详见 [CPU 运行回执](../evidence/ridge_execution_20260905.json) 和 [旧任务只读状态快照](../evidence/n14r_status_20260905.json)。累计 fit、runner 墙钟、全流程准备时间是不同量，不作云卡提速外推。

## 可报告证据

目前可报告的是容量、协议实现、模型块完成和运行成本。**没有读取先导或核心的识别分数、排名、效应、区间或 p 值。** 720 次 CPU 仅是核心的一半，未生成完整 completion/analysis_lock，不声称新方法有效。

## 问题及影响

CNN 的 720 个单元等待本机 N14R 完成、独立核验并释放 GPU。N14R 的现有自动化继续负责旧任务，本任务不重复控制其进程。现阶段最关键的剩余工作是 GPU 执行、完整闭合后的独立统计复算和写作，不是 Ridge 算力。

已启用本任务每小时续办与云算力预警。若 9/7 晚 GPU 仍不可用于新任务，或按实测预计新核心计算和核验会超过 9/10 晚，提前通知用户具体租机方案。GPU 预选单卡 4090 24GB/5090 32GB，CPU 至少 8 vCPU 优先 16、内存至少 32GB 优先 64、SSD 50–100GB，先 1–2 小时验证吞吐；此时没有租赁费用。

## 下一步

等待期间继续补完整结果的独立统计复算与论文骨架。GPU 释放后先执行 8 个正式 CNN 单元只测时间，再按同一计划续跑。全部 1,440 单元完成后才评分、独立核对和作图。9/13–9/15 保留给分析、写作和人工复核。

## 等待 GPU 期间新增交付

- 新增独立统计复核器 `result_verify.py`：不调用评分器统计函数，从原始预测的混淆计数重新构建逐人 UAR，并用 bootstrap 频数权重与独立线性分位数算法复算全部 28 个量及 1,456 行逐人表。先审查完整 1,440 单元、attempt 状态机、哈希和环境闭合；当前不对真实研究评分。
- 新增 [英文 Study II 方法与结果占位稿](../../../paper/study2/STUDY2_DRAFT.md)。已写明固定预算的“人数—每人句子覆盖”联合取舍、既往结果暴露及条件性区间边界；所有效果表仍为 PENDING。
- 新增只读 `deadline_status.py` 和 [预警快照](../evidence/deadline_status_20260905.json)。从旧账本近期 20/50/100 次完成速度的最慢窗口推算，附加交接 6 小时、CNN 每单元至少 120 秒、新结果核验 6 小时。这些是规划余量，不是本轮 CNN 或云 GPU 实测。当前未触发租机条件。
- 预警审阅发现并修复：合法恢复 DONE 缺少耗时字段、并发读取末行及取时顺序、遗漏 Ridge 未完成或两次失败耗尽。这些情况现会补核元数据或明确告警，不能给出乐观的完整研究 ETA；11 项预警测试已通过独立复审。

独立统计复核器的 26 项生成夹具测试通过，包括完整 1,440 单元重放、重新封口后的统计/CSV/attempt/环境篡改、未闭合先拒绝以及输出路径保护。与既有执行、元数据和评分测试合并后，**122 项测试全部通过（56.67 秒）**。这验证程序在测试数据上的行为，真实核心结果尚未产生或核验。

没有新增实际 GPU 训练、没有读取科学结果，没有租赁费用；旧 N14R 继续由其原有任务负责。16 个冻结源码/规范文件保持原身份。
