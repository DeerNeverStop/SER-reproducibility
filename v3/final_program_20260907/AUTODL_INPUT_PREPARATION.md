# AutoDL输入包与本地解包验收

> **公开投影（2026-09-08）**：本文件删除了账户、运维身份或私有审阅入口；科学方法与结果保留。原始文稿存于受控私有归档，原 SHA-256 为 `ba76afb67c9e3aaa802b002af2f6fd0735239d2e7a1f1359c62773fdf1babb52`。公开版不是原冻结文件的字节副本；原科学代码、结果与计划未在此改写。

2026-09-07。本记录产生于云迁移准备阶段，当时本地正式训练继续运行。账户及个人认证状态不随公开包分发。本记录只证明迁移输入可整理、传输和重读，不是云端已开机、Linux兼容性、速度收益或正式实验完成的证据。

输入位于本机`D:/SER-final-program-20260907/autodl_20260907/staging`。5个包共1,203,389,044字节，解包payload共1,662,538,264字节：8,308条原计划音频、原始基座、43个冻结源文件、额外本地导入依赖、预定恢复工具、5个固定元数据文件及原计划/来源锁。没有正式账本、reservation、DONE、预测或训练后checkpoint；`phase_seed`不能直接启动为新的完整正式研究。

打包时逐文件核SHA与原计划/来源锁；根任务随后实际运行`verify_inputs.py`，通过全部5包与8,366个成员的精确清单、尺寸和SHA检查，并解包至全新目录`local_input_acceptance`。实际解包验收报告为`autodl_20260907/input_verification_local.json`，完成于16:56:51 UTC。之后从解包后的repo目录、隐藏CUDA的独立本机进程调用原`checked_plan`，通过完整384单位和原来源锁检查；记录在`local_input_acceptance/plan_check_local.json`。这次没有运行模型、训练或读取新科学成绩，也没有在Linux上测试。

关键传输锚：

- `INPUT_MANIFEST.json` SHA-256：`1350f3121c9212252e6b71168516834adac8755ba3209784436732f55f263659`。
- `SHA256SUMS` SHA-256：`1dc649215d4a9baf4bc3785ff5bd57089f56e5e5f7a6a786f87e4d694c8a4778`。
- 原正式来源锁：`cd32459cef1e09c5efe5ef4c9f3a918aeef5a0aac15f2e98731a60daa3f4d258`，没有新建或重写。

上传时应传**5个tar包、INPUT_MANIFEST.json、SHA256SUMS、pack_inputs.py、verify_inputs.py、README.md**；后二脚本及README也在校验清单中，不能只传包和manifest后宣称全部清单通过。云端音频映射与运行时版本参考已在代码输入包内。使用包内README的全新目标解包和CPU计划检查命令；再次运行时换全新的目标目录与报告，不覆盖原验收。

未来迁移须先实测环境、输入传输及技术运行时间，再比较包含结果回传的总剩余时间。只有实际有收益时才交接：确认旧唯一写入进程退出，保存并核对完整旧账本和已封存单位，只分配未完成单位，保留任何中断尝试，回传完整新工件并核对 SHA。该历史输入包不支持重复已完成单位，也不包含可凭空重建旧运行历史的材料。
