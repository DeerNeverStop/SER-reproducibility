numpy>=2.4
scipy>=1.18
scikit-learn>=1.9
librosa>=0.11
soundfile>=0.14
joblib>=1.3
tqdm>=4.65
matplotlib>=3.10
pandas>=2.3
pypdf>=5.0
reportlab>=4.0

# The frozen runs used torch 2.11.0+cu128. Install the matching CUDA wheel
# from https://download.pytorch.org/whl/cu128 before running P1 or D1.
torch>=2.11

# Optional: enables voice-quality features (jitter / shimmer / HNR).
# If you skip it, the pipeline still runs without those 3 features.
praat-parselmouth>=0.4
