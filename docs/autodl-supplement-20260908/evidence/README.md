# 已验收结果与核验凭据

> **公开投影（2026-09-08）**：本文件删除了账户、运维身份或私有审阅入口；科学方法与结果保留。原始文稿存于受控私有归档，原 SHA-256 为 `a4bac041973b9feae8bf06f0c7a543f629368b9735abd2dae0030edae0753cbc`。公开版不是原冻结文件的字节副本；原科学代码、结果与计划未在此改写。

本目录保存实际完成后的原件副本及明确标识的派生索引；原审计文件中的本地路径和历史状态按原字节保留。

- [派生总索引](EVIDENCE_SUMMARY.json)：绑定全部九个评分输出、独立复核和归档审计，记录十个冻结 Python 文件未变、720 正式单元、8 pilot、90 正常批次、6,216 个当前文件哈希及 20 份保留权重共 9,643,695,848 字节。
- [独立数值复核原件](independent_replay.json)：完整十项主检验及描述统计通过 199,780 个标量比较，最大绝对差 7.105427357601002e-15。[原输入 SHA 清单](independent_input_sha256.json) 绑定实际读取的字节。
- 独立归档审计原件（未随此公开快照分发；原件另行保留）：实际全量小文件、保留检查点和训练/备份/释放记录核验。它在云端收尾前生成，其中 `instance_stopped:false`、`instance_released:false` 是当时审计不执行云操作的历史记录，不能拿来覆盖随后云端状态。
- 个人财务、具体实例身份、关机／释放控制面原件不随此公开包分发。其省略不改变本页所列科学结果与历史核验范围。

[results](../results/) 是原评分器九个输出文件的逐字节副本；`OUTPUT_MANIFEST.json` 覆盖其余八个文件并保留原语义摘要。没有向该目录加入 README 或改写路径。独立复核脚本位于 [tools](../../../tools/autodl_supplement_20260908/independent_check/README.md)；归档运维工具不随此公开快照分发。

这些复核是独立实现的数值核对和归档审查，不是新数据、再次训练或外部团队盲复现。原始音频、逐轮预测数组及保留权重未加入 Git；完整正式和 pilot 本地备份继续保留。非样本权重在存在时已验证，在预测/元数据本地备份确认后依协议释放，不能声称每个训练权重永久存在。

个人账户余额和消费记录仅保留于受控私有归档。
