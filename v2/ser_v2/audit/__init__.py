"""Audit tooling (AUDIT arm): beacon ordering, endpoint dictionary, rater forms, scripted
adjudication, and the interval family. Frame construction uses the GitHub API on the author's
machine; everything here is local, deterministic, and testable."""
